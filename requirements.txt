# Core (no AI needed)
pymupdf>=1.24
reportlab>=4.0
pyspellchecker>=0.8
rapidfuzz>=3.0
python-docx>=1.1
pillow>=10
numpy>=1.24
pytesseract>=0.3.10
httpx>=0.27
# App UI
flet>=1.0,<2
pyttsx3>=2.90   # reading aloud with the computer's own voices
pywin32>=306; sys_platform == "win32"   # Windows speech (SAPI) used directly
# Optional
anthropic>=1.0   # only needed when using the Anthropic provider
# keyring>=24    # store API keys in the OS keychain
